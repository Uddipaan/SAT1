__author__ = 'uddipaan'


#sparse count algorithm implementation

class SparseCount:

    def __init__(self, prb_sat)
    
        self.sat = prb_sat
    
    alpha
    ln = math.log
    sqrt = math.sqrt
    delta = 1 - min_conf
    T = ((ln(1/delta))/alpha)*ln(n)

    i=0
    while (i <= n) :
        for t in range(1,T):
            #hashing to be done
        if 
